// Headless harness for aximesh -- no CUDA, no GUI, so it builds and runs in a
// fraction of the time AxiSim.exe takes. Drive generateMesh from main().

#include "aximesh/aximesh.h"

int main() {
	return 0;
}
